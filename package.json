{
  "name": "novella",
  "version": "1.0.0",
  "private": true,
  "description": "A calm, local-first workspace for writing novels.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "migrate:json": "node scripts/migrate-json-to-sqlite.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "openid-client": "^6.8.4"
  }
}
